А с UNION  все хорошо.
после UNION запрос заключаешь в скобки
Если WHERE за скобками второго запроса, тогда применяется для всех
Если внутри, только для текущего.

Применение UNION  исключает повторяющиеся записи, как DISTINCT
поэтому, если нужен вывод всех записей пишешь UNION ALL